# Newsletter-Templates

Lege hier vorbefüllte Newsletter-Templates als `.yml` oder `.yaml` ab.

Die Dateien können in der lokalen Entwicklungsumgebung über den zusätzlichen
Template-Export aus einem Newsletter erzeugt werden. Neue Nutzer erhalten beim
Anlegen ihres Accounts automatisch Kopien aller Templates aus diesem Ordner.
