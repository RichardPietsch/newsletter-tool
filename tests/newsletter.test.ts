import { describe, expect, it } from 'vitest'; import { createBlock, createDefaultDocument } from '@/lib/newsletter/defaults'; import { deleteBlock, insertBlock, moveBlock, updateBlock, History } from '@/lib/newsletter/operations'; import { allowedUrl, imageBlockSchema, newsletterDocumentSchema } from '@/lib/newsletter/schema'; import { renderNewsletter } from '@/email/render-newsletter';
describe('newsletter core',()=>{it('validates block schemas and defaults',()=>{const d=createDefaultDocument(); expect(newsletterDocumentSchema.parse(d).blocks.map(b=>b.type)).toEqual(['header','footer']); expect(imageBlockSchema.safeParse({id:'i',type:'image',src:'https://x.test/a.png',decorative:false}).success).toBe(false); expect(imageBlockSchema.safeParse({id:'i',type:'image',src:'https://x.test/a.png',decorative:true}).success).toBe(true)}); it('inserts deletes and moves only content blocks',()=>{let d=createDefaultDocument(); const t=createBlock('text'); d=insertBlock(d,1,t); expect(d.blocks[1].id).toBe(t.id); expect(deleteBlock(d,d.blocks[0].id)).toBe(d); d=moveBlock(d,t.id,1); expect(d.blocks[1].id).toBe(t.id); d=deleteBlock(d,t.id); expect(d.blocks).toHaveLength(2)}); it('undo redo works',()=>{const d=createDefaultDocument(); const h=new History(d); const changed=insertBlock(d,1,createBlock('event')); h.commit(changed); expect(h.undo().blocks).toHaveLength(2); expect(h.redo().blocks).toHaveLength(3); h.commit(updateBlock(changed,changed.blocks[1].id,{title:'X'} as any)); expect((h.present.blocks[1] as any).title).toBe('X')}); it('validates links',()=>{expect(allowedUrl('https://example.com')).toBe(true); expect(allowedUrl('javascript:alert(1)')).toBe(false)}); it('renders complete html without tailwind output',()=>{let d=createDefaultDocument('Test'); d=insertBlock(d,1,createBlock('text')); const html=renderNewsletter(d); expect(html.toLowerCase()).toContain('<!doctype html>'); expect(html).toContain('lang="de"'); expect(html).toContain('<body'); expect(html).not.toMatch(/class="(?:flex|p-|text-|bg-)/)})});
